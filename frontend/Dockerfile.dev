# Frontend Dockerfile (Development) - Com Hot Reload
FROM node:18-alpine

WORKDIR /app

# Copiar package.json e package-lock.json
COPY package*.json ./

# Instalar dependências
RUN npm install

# Expor porta do React Dev Server
EXPOSE 3000

# Variável de ambiente para evitar erro de hot reload
ENV WATCHPACK_POLLING=true
ENV CHOKIDAR_USEPOLLING=true

# Comando para iniciar React dev server
CMD ["npm", "start"]
